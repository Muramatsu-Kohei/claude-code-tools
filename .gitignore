# Claude Code の個人用ローカル設定(権限の許可リストなど。環境ごとに異なるため共有しない)
.claude/settings.local.json
**/.claude/settings.local.json

node_modules/

# 手元でしか意味を持たない作業ログ・実測値(開発日記の類)。
# 公開ドキュメントには「なぜその設計になったか」だけを残し、進捗チェックリストや
# 自分の環境の数値はこちらに置く。どちらも陳腐化するうえ読者の判断材料にならないため。
*.local.md

# OS が勝手に作るもの
.DS_Store
Thumbs.db
